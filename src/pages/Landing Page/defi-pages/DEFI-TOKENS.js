import React from 'react'
import GeneralizedComponent from './Generalized';


const HEX = () => {
    return <GeneralizedComponent title="HEX" />;
};

const LOAN = () => {
    return <GeneralizedComponent title="LOAN" />;
};

const PTGC = () => {
    return <GeneralizedComponent title="PTGC" />;
};

const REX = () => {
    return <GeneralizedComponent title="REX" />;
};

const TEXAN = () => {
    return <GeneralizedComponent title="TEXAN" />;
};


const WATT = () => {
    return <GeneralizedComponent title="WATT" />;
};


export { HEX, LOAN, PTGC, REX, TEXAN, WATT };